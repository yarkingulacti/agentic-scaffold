#!/usr/bin/env bash
# Post-session hook — runs after every successful coding session.
# Customize this file with project-specific session-close tasks.
#
# Available context variables:
#   projectName    = "py-app"
#   packageManager = ""
#   scriptLanguage = "node"

set -euo pipefail

echo "Running post-session tasks for py-app..."
